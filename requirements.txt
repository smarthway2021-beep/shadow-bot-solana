# Shadow Bot Solana - Dependencies
# Python 3.11+
# Instalar com: pip install -r requirements.txt

# HTTP / Async (funciona direto no Windows)
aiohttp==3.9.5
httpx==0.27.0
websockets==12.0

# Telegram
python-telegram-bot==21.3

# Utils
python-dotenv==1.0.1
rich==13.7.1
loguru==0.7.2
pydantic==2.7.1
pydantic-settings==2.3.1

# Database
sqlite-utils==3.36

# Solana RPC via HTTP puro (sem compilacao Rust)
# solana-py e solders requerem Rust no Windows
# O bot usa Helius via HTTP diretamente (aiohttp)
# Instale separadamente se quiser:
# pip install solders==0.21.0
# pip install solana==0.34.3
